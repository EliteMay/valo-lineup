# 作業報告書

## 2026-08-26 / v0.1

### 依頼内容

- `strats.gg/valorant/lineups` を参考にしたVALORANT定点サイトを作成。
- 自分で定点を作れる専用タブを追加。
- 可能な範囲で品質を最大化。
- 見た目を重視。
- 利用可能ならVALORANTのエージェント画像などを使用。

### 要件定義

#### 目的
VALORANTの定点を素早く見つけ、マップ上で軌道を理解し、自分専用の定点も同じUIで管理できるようにする。

#### 使用者
主にPCでVALORANTをプレイし、別画面や試合前に定点を確認する個人ユーザー。

#### 必要機能
- マップ上で定点閲覧
- 各種フィルター
- 軌道表示
- 同一/近接着弾点グループ
- 詳細表示
- お気に入り
- 自作定点
- 自作定点編集/削除
- 画像/動画/メモ
- JSONバックアップ/復元

#### 画面構成
1. 定点ライブラリ
2. 自分の定点を作る
3. マイ定点
4. データ管理モーダル

#### データ構成
- 配布デモ定点: `data/lineups.json`
- 自作定点: localStorage JSON
- エージェント/マップ: 起動時にValorant-APIから取得

#### 保存方法
ブラウザ localStorage。
参考画像はWebPへ縮小圧縮してData URLとしてJSON内へ保存。

#### 完成条件
- 起動できる
- 閲覧フィルターが動く設計
- マップ軌道表示がある
- 自分でルートを作れる
- 保存・編集・削除がある
- JSONバックアップがある
- READMEと作業報告がある
- npm追加依存なし

### 調査した内容

Strats.ggの公開ページと公開説明から、以下の特徴を確認して設計へ反映。

- マップ / エージェント / スキル / 攻守 / 難易度での絞り込み
- インタラクティブマップ
- 開始地点から着弾地点までの軌道
- バウンドを含む軌道
- 同じ着弾地点の定点グループ化
- 動画チュートリアルという構成

ただし、Strats.ggのHTML/CSS/ロゴ/投稿済み定点データはコピーしていない。

### 実装内容

#### UI
- ダークネイビー + VALORANT系レッドの独自UI
- 左アイコンナビ
- 3カラム定点ライブラリ
- フィルターパネル
- 大型マップ
- 詳細パネル
- カード型一覧
- 自作専用ビルダー
- マイ定点カード
- データ管理モーダル
- トースト通知
- レスポンシブレイアウト

#### マップ
- 画像と座標レイヤーを同じ正方形領域へ自動調整
- 0〜100%正規化座標
- SVG軌道
- 開始点
- 中継点
- 着弾点
- 近接着弾点のグループ表示

#### 自作定点
- 立ち位置クリック
- 中継点複数
- 着弾点クリック
- マーカーのドラッグ微調整
- 1点戻す
- ルート全消去
- 参考画像3枚
- 自動画像圧縮
- 動画URL
- メモ
- タグ
- 編集

#### 管理
- お気に入り
- マイ定点検索
- 編集
- 削除
- JSON export/import
- 全削除

#### 外部素材
Valorant-APIの公開データを起動時に取得する構成とした。
ZIPへ大量のゲーム素材を転載せず、画像URLを利用。

### 検証

実施済み:
- `node --check assets/js/app.js`
- `node --check assets/js/storage.js`
- `node --check server.js`
- Nodeローカルサーバー起動確認
- `index.html` HTTP 200確認
- `data/lineups.json` JSON構文確認
- HTML内ID重複確認
- UTF-8で作成
- `start.bat` に `chcp 65001` を設定

### 確認できなかったこと

この作業環境では外部インターネット通信を行うブラウザ実機テストが制限されていたため、以下はコード上の実装確認まで。

- Valorant-APIの実ブラウザ上での画像読込
- 全エージェント画像の実表示
- 全マップ画像の実表示
- Windows実機での `start.bat` ダブルクリック動作

Chromiumのヘッドレススクリーンショット検証も、作業環境側Chromiumが終了しない問題により取得できなかった。

### 未実装 / 今後の候補

- クラウド同期
- Riot IDログイン
- 投稿共有
- 複数ユーザー
- 本物の実戦定点データベース
- 動画ファイルそのもののローカル保存
- 画像をlocalStorageではなくIndexedDBへ移行して大容量化
- 定点ごとのチャージ量、ジャンプ投げ、走り投げ等の専用入力欄
- マップのサイト/エリア名をクリックできるコールアウト表示

### 重要な注意

配布済み `data/lineups.json` はUIデモ用であり、実戦で正確に機能する定点として検証したデータではない。


## v0.2 追加作業
- 自作定点の画像登録を `Win + Shift + S` → `Ctrl + V` 中心の操作へ変更。
- 立ち位置 / 合わせ場所 / 着弾結果から貼り付け先を選択可能にした。
- ページ全体の paste イベントから画像を取得し、選択中スロットへ登録する処理を追加。
- Clipboard API が利用可能な環境向けに「貼る」ボタンを追加。
- ファイル選択はフォールバックとして維持。
- 各画像に削除ボタンを追加。
- スクリーンショット登録後も従来通りWebPへ圧縮してブラウザ保存する。

### 制約
通常のWebページからWindowsの `Win + Shift + S` キー操作そのものを安全・確実に強制実行することはできないため、Windows標準の切り取りをユーザーが実行し、その後 `Ctrl + V` で取り込む方式としている。

## 2026-08-27 / v0.3 GitHub Pages対応

### 依頼内容
- v0.2をGitHub方式へ切り替える。
- GitHub PagesでURLを開くだけで利用できる構成にする。
- GitHubで動かないローカル専用要素を整理する。
- 既存の自作定点・スクリーンショット・JSON管理機能は維持する。

### 確認した既存仕様
- HTML/CSS/JS/JSONだけで主要機能が構成されている。
- `assets/` と `data/` は相対パス参照。
- `data/lineups.json` は `fetch('data/lineups.json')` で取得。
- VALORANT素材は `https://valorant-api.com/` から取得。
- 自作データはlocalStorage保存。
- Win + Shift + S後のCtrl + Vはページのpasteイベントで処理。

### 変更内容
- `server.js` をGitHub Pages版から削除。
- `start.bat` をGitHub Pages版から削除。
- `.nojekyll` を追加。
- `.gitignore` を追加。
- ZIP内の余分な親フォルダをなくし、リポジトリ直下へそのまま配置できる構成へ変更。
- READMEをGitHub Pages前提へ全面更新。
- READMEにPages公開手順、localStorageのURL依存、JSONバックアップ注意点を追加。
- 崩してはいけない仕様をGitHub Pages版に更新。

### 維持した機能
- 定点ライブラリ
- マップ / エージェント / アビリティ / 攻守 / 難易度フィルター
- 軌道表示
- 自作定点
- 立ち位置 / 中継点 / 着弾点
- ドラッグ微調整
- Win + Shift + S → Ctrl + V
- Clipboard API貼り付けボタン
- 画像ファイル選択
- WebP圧縮
- お気に入り
- 編集 / 削除
- JSON export / import
- localStorage保存
- Valorant-API取得とフォールバック

### GitHub Pages互換性
- HTML/CSS/JS/JSONのみで動作する静的構成。
- `/assets/...` のようなルート絶対パスを使用せず、`assets/...` / `data/...` の相対パスを維持。
- プロジェクトページ `https://USER.github.io/REPO/` のようなサブパスでも参照が壊れにくい構成。
- サーバーサイド機能、秘密鍵、APIキーは使用していない。

### 注意点
- 自作定点はGitHubへ保存されず、閲覧中ブラウザのlocalStorageへ保存される。
- localStorageはオリジン単位で保存される。同じ `USER.github.io` 配下ではリポジトリパスが変わっても共有され、別ドメインへ移すと引き継がれないためJSONバックアップを利用する。
- Valorant-API画像表示にはインターネット接続が必要。
- GitHub Pages上の実URLでの最終表示確認は、公開先リポジトリがまだLineup Lab用として用意されていないため未実施。
