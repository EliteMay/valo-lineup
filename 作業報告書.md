# 作業報告書

## 変更履歴まとめ

### 2026-08-27 / v0.4 GitHub共有JSON対応
- `data/lineups.json` を友達全員で読む共通定点データとして運用。
- GitHub上の共有定点とブラウザ内のマイ定点をまとめた `lineups.json` 書き出しに対応。
- 自作途中・お気に入り・設定はlocalStorageを維持。

### 2026-08-27 / v0.5 MP4対応
- PC内MP4を定点へ登録可能にした。
- MP4本体はJSON/localStorageへ入れずIndexedDBへ保存。
- 共有MP4は `assets/videos/` に置き、定点データはURLだけ保持。
- 詳細画面でHTML5 video再生に対応。

### 2026-08-27 / v0.6 GitHub向けMP4自動圧縮
- `ffmpeg.wasm` を使った24MB以下目標の自動圧縮を追加。
- H.264 / AAC、最大60fps、動画時間からビットレート・最大解像度を自動調整。
- 1回目で24MBを超えた場合は1回だけ自動再圧縮。
- 通常閲覧ではFFmpeg coreを読み込まず、圧縮時のみ取得。
- 圧縮本体は `compressor-core.js` に分離。

### 2026-08-27 / v0.7 プレミアムUI全面調整
- 暗色UIの階層、余白、文字サイズ、ボタン、入力欄、マップ、詳細、作成画面、モーダルを統一。
- 見た目改善によって既存定点データやMP4圧縮ロジックを変更しない構成を採用。

---

## 2026-08-27 / v0.8 MAP・定点カード・詳細パネル再設計

### 依頼内容
- v0.7でも残っていた「個人制作の管理画面っぽさ」「安っぽさ」をさらに減らす。
- おすすめ構成でサイトの見た目を改善する。
- 既存機能・軽量性・GitHub Pages対応を崩さない。

### 要件定義

#### 目的
サイトを単なる濃色ダッシュボードではなく、VALORANTの定点を探すこと自体が主役になる攻略ツールUIへ近づける。

#### 使用者
友達同士でLineup Labを利用するユーザー。

#### 必要機能
- 中央MAPを視覚的な主役にする。
- 定点カードに参考画像を使う。
- 参考画像がない定点も空白カードにしない。
- 右詳細パネルの情報密度とビジュアルを改善する。
- マップ選択・エージェント選択の詰まり感を減らす。
- エージェント検索を追加する。
- 既存作成・MP4・圧縮・共有JSONを維持する。

#### 画面構成
- 左: フィルター
- 中央: 大型MAP + 定点カード
- 右: 選択中定点の詳細
- 作成画面 / マイ定点 / データ管理も同じデザインシステムへ合わせる。

#### データ構成
既存構造を変更しない。

カードサムネイルは既存の以下を優先して使用する。

1. `images.result`
2. `images.aim`
3. `images.standing`
4. 画像がなければ現在マップ画像

#### 保存方法
変更なし。

- 共通定点: `data/lineups.json`
- マイ定点: localStorage
- ローカルMP4: IndexedDB
- 共有MP4: `assets/videos/`

#### 崩してはいけない仕様
- GitHub Pagesだけで動く。
- Node.js / npmを必須にしない。
- `data/lineups.json` 共有を維持。
- localStorage / IndexedDBの保存形式を変更しない。
- Win + Shift + S → Ctrl + Vを維持。
- MP4登録 / 自動圧縮を維持。
- YouTube / 動画URLを維持。
- フィルター / お気に入り / 編集 / 削除を維持。
- 見た目のために主要機能を削除しない。
- 重い常時アニメーションを増やさない。

#### 完成条件
- v0.8 UIがサイト起動時に読み込まれる。
- MAPがv0.7より大きく・明るく表示される。
- 定点カードに参考画像またはマップ画像が表示される設計になる。
- エージェント検索が使える設計になる。
- 右詳細のヒーロー・参考画像領域が改善される。
- UI装飾監視が無限再描画を起こさない。
- README / 作業報告書が現行構成へ更新される。

### スクリーンショットから確認した主な問題
- 中央MAP自体より背景模様・空白が目立っていた。
- 左フィルターでマップとエージェントが小さいタイルとして大量に並び、詰まり感があった。
- 右詳細は上部以外の空白が大きく、情報量が少なく見えた。
- 定点一覧がキャラアイコン + 文字中心で、定点サイトらしいビジュアルが弱かった。
- 1pxの枠線が多数の要素に使われ、すべて同じ箱に見えやすかった。
- 文字が小さい箇所が残っていた。

### 今回変更した内容

#### 中央MAP
- MAP表示領域を拡大。
- マップ画像のopacity / contrast / brightnessを調整。
- マップ画像・ルート・ピンの座標レイヤーを同じ比率で拡大。
- 背景グリッドを弱くしてマップ自体を優先。
- ビネットを追加して中央へ視線を集める。
- 選択ルートと着弾ピンを視認しやすく調整。
- `TACTICAL MAP / START → LAND` の控えめな補助表示を追加。

#### 定点カード
- 文字中心の横長カードからビジュアル付きカードへ変更。
- `result → aim → standing` の順で参考画像をカードサムネイルへ使用。
- 参考画像がない定点ではマップ画像を使用。
- エージェント画像をサムネイル上へ重ねる。
- アビリティ名をサムネイル内へ表示。
- 選択中カード・hover状態を枠線ではなく背景差、影、赤アクセントで表現。

#### 左フィルター
- マップ一覧を大きめの横スクロールカードへ変更。
- エージェント検索欄を追加。
- エージェント一覧に最大高さを設定し、スクロール可能に変更。
- マップ / エージェント / アビリティの選択状態を整理。

#### 右詳細パネル
- エージェントヒーローを大型化。
- タイトルサイズと余白を改善。
- 参考画像3種のサイズ配分を調整。
- ルート / メモ / 動画の面の階層を整理。
- `LINEUP DATA` の補助ラベルを追加。

#### 全体
- 青寄りの濃紺を減らし、黒・チャコール主体へ調整。
- 1px枠線の多用を減らし、面の明度差と影を中心に変更。
- 小さすぎる文字を減らし、主な操作文字を11〜13px中心へ変更。
- 作成画面、MP4、圧縮、マイ定点、データ管理モーダルも同じ質感へ調整。
- 1550 / 1280 / 980 / 720px帯でレイアウト調整。

### 実装上の対応
- `assets/css/premium-v2.css` を追加。
- `assets/js/premium-ui-v2.js` を追加。
- `compressor.js` のUI読込先を `premium-ui-v2.js` へ変更。
- 圧縮本体 `compressor-core.js` は変更していない。
- 未使用になった旧 `assets/js/premium-ui.js` を削除。
- 定点カード補助処理は `data/lineups.json` と `LineupStorage.getUserLineups()` から既存データを読むだけで、保存形式は変更しない。
- MutationObserverが自分のDOM変更を再検出し続けないよう、カードごとにsignatureを保存し、内容が同じ場合はDOMを書き換えない処理を追加。

### 変更したファイル
- `assets/css/premium-v2.css` 追加
- `assets/js/premium-ui-v2.js` 追加
- `assets/js/compressor.js` 更新
- `assets/js/premium-ui.js` 削除
- `README.md` 更新
- `作業報告書.md` 更新

### 修正した不具合 / リスク
- UI補助処理がMutationObserver経由で再描画ループする可能性を修正。
- 旧UIと新UIが二重で適用される可能性を、ローダー切替と旧ファイル削除で防止。

### 追加した機能
- エージェント名検索。
- 定点カードの自動サムネイル表示。
- 画像がない定点のマップ + エージェント補完表示。

### 未完了の項目
- 実際のGitHub Pages公開画面を見ながらの最終ピクセル調整。

### できなかったこと
- この環境からWindows実機上のFirefox / Chromiumで直接操作して画面比較すること。

### 確認できたこと
- v0.8用CSS / JSがGitHubへ追加された。
- `compressor.js` が `premium-ui-v2.js` → `compressor-core.js` の順で読み込む構成になった。
- 旧 `premium-ui.js` は削除した。
- 既存 `app.js` / `storage.js` / `data/lineups.json` / `compressor-core.js` の保存・主要ロジックは変更していない。
- GitHub Pages用の相対パス構成を維持している。

### 確認できなかったこと
- GitHub Pages上でのv0.8最終視覚確認。
- Firefox / Chromium両方でのレイアウト最終確認。
- 全マップ / 全エージェント / 大量定点を表示した際の最終スクロール感。
- Windows実機でのMP4圧縮完走は引き続き未確認。

### 今後必要な作業
- GitHub Pages反映後にスクリーンショットで確認する。
- MAPサイズ、カード高さ、右詳細幅を実画面に合わせて必要なら微調整する。
