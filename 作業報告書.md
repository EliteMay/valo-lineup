# Lineup Lab 作業報告書

## 現在バージョン

**v1.1.0 / 2026-08-30**

## 今回の目的

v1.0監査で残った最重要課題だった **画像Data URLのlocalStorage保存** を解消し、定点数・スクリーンショット数が増えても容量破綻しにくい保存構造へ移行しました。

同時に、共有時にローカル専用画像参照がGitHubへ混入しない仕組みと、自動検証を強化しました。

---

## 今回変更した内容

### 1. 画像本体をIndexedDBへ移行

追加:

- `assets/js/media-store.js`
- `sw.js`

保存先:

```text
IndexedDB
DB: lineupLab.images.v1
store: images
```

localStorageには画像本体ではなく、以下のような軽い参照を保存します。

```text
_lineup-media/<lineup-id>/standing.webp
_lineup-media/<lineup-id>/aim.webp
_lineup-media/<lineup-id>/result.webp
```

### 2. Service Workerで画像を表示

`sw.js` が `_lineup-media/` への画像リクエストを受け取り、IndexedDBのBlobを返します。

この方式により、既存の `app.js` が `<img src="...">` で画像を表示する仕様を大きく壊さずIndexedDB化できました。

GitHub Pagesのサブパスにも対応するよう、Service Workerのscopeは `document.baseURI` 基準です。

### 3. 旧Data URLを自動移行

既存マイ定点にData URL画像が残っている場合、Service Worker / IndexedDBが利用可能なら初回起動時に:

1. 画像をIndexedDBへ保存
2. localStorageのData URLを `_lineup-media/` 参照へ置換
3. 次回以降はService Workerから画像配信

します。

利用できない環境では従来方式へフォールバックします。

### 4. 画像・MP4の孤児データ削除

`LineupStorage.saveUserLineups` / `clearUserLineups` をメディア基盤でラップし、参照されなくなった:

- IndexedDB画像
- IndexedDB MP4

を掃除します。

### 5. Foundationをローダー化

変更前:

```text
foundation-v1.js
└─ premium-ui-v2.js
```

変更後:

```text
foundation-v1.js
├─ media-store.js
└─ foundation-core-v1.js
   └─ premium-ui-v2.js
```

追加:

- `assets/js/foundation-core-v1.js`

`foundation-v1.js` はv1.1用の薄いローダーになりました。

メディア基盤をFoundation本体より先に読み込み、v1.0のlocalStorage容量ガードが画像Data URLを誤って止めないようにしています。

### 6. 共有画像用ディレクトリ

追加:

```text
assets/lineups/.gitkeep
```

共有時の画像配置:

```text
assets/lineups/<lineup-id>/standing.webp
assets/lineups/<lineup-id>/aim.webp
assets/lineups/<lineup-id>/result.webp
```

### 7. 共有パッケージZIP

「共有用JSONを書き出す」を、画像も扱える **共有パッケージを書き出す** へ強化しました。

ZIP内容:

```text
lineups.json
assets/lineups/<id>/standing.webp
assets/lineups/<id>/aim.webp
assets/lineups/<id>/result.webp
README.txt
```

ローカル専用 `_lineup-media/` 参照は共有JSONへそのまま出さず、`assets/lineups/` の相対パスへ変換します。

MP4はファイルサイズが大きいためZIPへ自動同梱せず、従来どおり `assets/videos/` へ手動アップロードします。

### 8. ZIPを外部ライブラリなしで生成

npm / JSZip等を追加せず、`media-store.js` 内に最小限のZIP生成処理を実装しました。

- Store方式（無圧縮）
- CRC32計算
- UTF-8ファイル名

画像自体がWebPなので、ZIP側で再圧縮しない構成です。

### 9. 共有JSON検証を強化

`tests/validate-data.mjs` を更新し、以下もエラーにしました。

- Base64 / Data URL画像が共有JSONへ入る
- `_lineup-media/` ローカル参照が共有JSONへ入る

### 10. GitHub Actionsをプロジェクト検証へ拡張

`.github/workflows/validate.yml` を更新しました。

検証対象:

- `data/lineups.json`
- classic JS構文
- ES module JS構文
- `sw.js`

現在のGitHub Actions実行で **Validate Lineup Project = success** を確認しています。

---

## 今回変更したファイル

追加:

- `assets/js/media-store.js`
- `assets/js/foundation-core-v1.js`
- `assets/lineups/.gitkeep`
- `sw.js`

変更:

- `assets/js/foundation-v1.js`
- `tests/validate-data.mjs`
- `.github/workflows/validate.yml`
- `README.md`
- `作業報告書.md`

---

## 維持した機能

- GitHub Pages
- `data/lineups.json` トップレベル配列
- マップ / エージェント / アビリティフィルター
- マップ上の定点表示
- 定点作成 / 編集 / 削除
- `Win + Shift + S` → `Ctrl + V`
- 画像ファイル選択
- YouTube / 動画URL
- IndexedDB MP4
- MP4自動圧縮
- GitHub共有ON/OFF
- お気に入り
- モバイル / 狭画面ドロワー

---

## 修正した問題

- 画像Data URLがlocalStorageを急速に圧迫する
- 共有JSONへローカル専用画像参照が混ざる可能性
- 画像削除後もIndexedDBへ残骸が残る設計
- v1.0画像保存方式と共有方式が分離していた
- GitHub ActionsがJSONだけでJS構文を見ていなかった

---

## 未完了の項目

### 1. app.js / premium-ui-v2.js の統合

カードサムネイル等はまだ `premium-ui-v2.js` のMutationObserver方式です。

次の大きい保守性改善は、カード・詳細・検索UIを `app.js` 側の正式render処理へ統合することです。

### 2. 実用定点データ

`data/lineups.json` はデモ中心です。

未確認の定点を推測して追加していません。

### 3. index.htmlキャッシュキー

`index.html` 側のFoundation URLはv1.0時のクエリ文字列が残っています。GitHub Pages側ではファイル更新されますが、ブラウザキャッシュが強い場合は `Ctrl + F5` が必要になる可能性があります。

次回HTML整理時にv1.1へ揃える予定です。

---

## できなかったこと / 未確認

- Windows実機ブラウザで旧Data URL → IndexedDB移行完走
- Service Worker経由で再読み込み後も3画像が表示されること
- Firefox / Chromium両方での実機確認
- 共有ZIPを展開してGitHubへ反映後、別PCから画像表示できること
- 大量画像100件以上の長期容量試験
- Windows実機でffmpeg.wasm圧縮完走

未確認項目は確認済みとして扱いません。

---

## 確認したこと

- `media-store.js` がGitHubに存在する
- `sw.js` がGitHubに存在する
- `assets/lineups/` が存在する
- `foundation-v1.js` がv1.1ローダー化されている
- `foundation-core-v1.js` が存在する
- 共有JSON検証にData URL / `_lineup-media/` チェックがある
- GitHub Actionsで主要JS構文チェックが成功
- GitHub Pagesビルドが成功

---

## 今後必要な作業

優先順:

1. GitHub Pages実機で画像3枚を登録
2. ページ再読み込み後に画像が残るか確認
3. 元スクリーンショット削除後も表示できるか確認
4. 共有ZIPをGitHubへ反映し別ブラウザで確認
5. `index.html` キャッシュキーをv1.1へ統一
6. `app.js` / `premium-ui-v2.js` 統合
7. 実用定点データ追加

---

## 変更履歴

- v0.1 — 初版
- v0.2 — スクリーンショット改善
- v0.3 — GitHub Pages
- v0.4 — 共有JSON
- v0.5 — MP4 / IndexedDB
- v0.6 — MP4自動圧縮
- v0.7 — Premium UI
- v0.8 — MAP / カード / 詳細再設計
- v0.9 — 他プロジェクトのUIパターン導入
- v1.0.0 — 監査結果を反映した基盤修正
- **v1.1.0 / 2026-08-30 — 画像IndexedDB / Service Worker / 共有画像ZIP**