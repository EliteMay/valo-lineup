# Lineup Lab 作業報告書

## 現在バージョン

**v1.2.0 / 2026-08-31**

## 今回の目的

見た目を最優先で再設計しました。

対象は単なる色変更ではなく、次の順で見直しています。

1. Information Architecture
2. Layout / Grid
3. Typography
4. Spacing
5. Visual Hierarchy
6. Component consistency
7. Responsive behavior
8. Color / Effect

Design Directionは **Tactical Workspace** です。

「VALORANT風の派手な画面」ではなく、定点を探す・見る・作るための競技用戦術ツールとして成立することを優先しました。

---

## Design Concept

### Signature

中央の **TACTICAL MAP** を唯一の強いSignatureにしました。

- Mapを最も大きいVisualへする
- Map専用の戦術フレームを追加
- 背景装飾を弱くする
- Route / Start / Landを見やすくする
- 他のPanelはMapより弱くする

### Color Rule

- Background: 黒〜Graphite
- Main text: Neutral white
- Secondary: Gray
- Accent: `#ff4655`
- Cyan: Route / Defense等の補助Semantic用途

Accent redを全面へ広げず、Selected / Primary actionへ限定しています。

### Shape Rule

以前より角丸を減らし、4〜8pxを中心にしました。

- Panel: 6〜8px
- Button / Input: 4〜5px
- Tag: 3px
- Tactical Map: 6px

ShadowもOverlay / Map等の必要な場所だけに限定しました。

---

## 今回変更した内容

### 1. 新しいVisual Source of Truth

追加:

```text
assets/css/visual.css
```

v1.2の最終Visual layerです。

`foundation-v1.js` が最後に読み込むため、v1.0 / v1.1の保存・共有機能を維持したまま見た目を統一します。

### 2. App Shell

- Railを72pxへ整理
- Navigation iconを44pxへ統一
- 選択中Navigationは左2pxのAccent line
- Topbarを76pxへ整理
- Tabsを大きいPillからFlat tabへ変更
- 不要なShadowを削減

### 3. Filters

- PanelをGraphite Surfaceへ変更
- Map pickerを小さいFilmstripへ変更
- Agent pickerを5列へ変更
- Selected stateをBorder + bottom Accentへ統一
- Input / Select / Segmented controlのShapeを統一

### 4. Tactical Map

- Mapを画面で最も強いVisualへ変更
- 内側にTactical frameを追加
- Grid背景をさらに弱く変更
- Map画像のContrast / Brightnessを調整
- Route lineを細く整理
- 選択中RouteはAccent red
- Legendを小さく整理

### 5. Lineup cards

- 大きいShadow / Rounded cardを廃止
- 画像が主役の矩形カードへ変更
- Hoverで大きく浮かせない
- Selected cardは左Accent line
- Metadata tagを小さいRect badgeへ変更

### 6. Detail Inspector

- 右PanelをInspectorとして再設計
- Agent heroを維持しながらEffectを削減
- Route / Notes / Referenceを細いDividerで整理
- Screenshot thumbを矩形寄りへ変更
- Video / MP4も同じShape languageへ統一

### 7. Creator Workbench

- 左Form / 右MapのWorkbench感を強化
- Form sectionを小さい番号 + Divider構成へ変更
- Route mode / Upload slot / MP4 UIを同じSurfaceへ統一
- Creator MapもLibrary Mapと同じVisual languageへ統一

### 8. My Lineups / Modal

- Heroを大きいGradient cardからFlat surfaceへ変更
- Mine cardのShadowを削除
- Data modal / Settingsを同じ4〜7px Shapeへ統一

### 9. Responsive

- 1580px以下: 左 / 右Panelを少し縮小
- 1380px以下: Detailを下段へ回し、Map幅を優先
- 1180px以下: 既存Nav drawer / Filter drawerを維持
- 760px以下: Card / Map / Media gridを1列寄りへ変更

---

## 変更したファイル

追加:

- `assets/css/visual.css`

変更:

- `assets/js/foundation-v1.js`
- `README.md`
- `作業報告書.md`

## 維持した機能

- GitHub Pages
- `data/lineups.json`
- Map / Agent / Ability / Side / Difficulty filter
- 定点作成 / 編集 / 削除
- Screenshot paste
- IndexedDB画像
- Service Worker画像配信
- MP4 / YouTube
- MP4自動圧縮
- GitHub共有ON/OFF
- 共有パッケージZIP
- お気に入り
- JSON Validation
- Nav / Filter drawer

---

## 今回変更していないもの

- 保存Schema
- localStorage key
- IndexedDB DB / store名
- `data/lineups.json`形式
- MP4圧縮設定
- 共有ZIP形式
- Service Worker画像配信方式

見た目更新で保存互換性を壊さないため、上記は変更していません。

---

## 現在残っている重要課題

### app.js / premium-ui-v2.js

Card thumbnail / Agent search / Detail decorationの一部はまだMutationObserver方式です。

Visual更新とは分離し、次回以降のArchitecture作業で `app.js` 正式renderへ統合する方が安全です。

### 実用定点データ

現在はDemo中心です。

正しい照準・Charge・Bounceを未確認のまま追加していません。

### Cache key

`index.html` は旧Query stringを持つ箇所があります。通常はPages更新で再検証されますが、強いBrowser cacheが残る場合はHard Reloadが必要になる可能性があります。

---

## 確認すること

### Repository / CI

- `visual.css` がGitHubに存在すること
- `foundation-v1.js` が `visual.css?v=1.2.0` を読み込むこと
- JavaScript構文Validation
- GitHub Pages build

### Visual Review

実際のPages描画で以下を確認します。

- MAPが最優先Visualになっているか
- Filter / DetailがMAPを食っていないか
- Textが小さすぎないか
- CardがAI Template的な大量Card表現に戻っていないか
- 1280〜1600pxでMAPが潰れないか
- Hover / selected / focusが区別できるか
- 760px以下で致命的なOverflowがないか

未確認項目は確認済みとして扱いません。

---

## 変更履歴

- v0.1 — 初版
- v0.2 — スクリーンショット改善
- v0.3 — GitHub Pages
- v0.4 — 共有JSON
- v0.5 — MP4 / IndexedDB
- v0.6 — MP4自動圧縮
- v0.7 — Premium UI
- v0.8 — MAP / カード / 詳細再設計
- v0.9 — 他ProjectのUI Pattern導入
- v1.0.0 — 保存 / 共有 / UI / Validation基盤整理
- v1.1.0 — 画像IndexedDB / Service Worker / 共有画像ZIP
- **v1.2.0 — Tactical Workspace Visual Redesign**
