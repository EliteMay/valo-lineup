# 作業報告書

## 2026-08-27 / v0.4 GitHub共有JSON対応

### 依頼内容
- 友達同士で同じ定点を見られるようにする。
- GitHubの `data/lineups.json` を全員共通データとして使う。
- アカウント、投稿申請、外部DBは使わない。

### 変更内容
- `data/lineups.json` を全員共通定点として扱う方針を明確化。
- サイト起動時の共有JSON読み込みを維持。
- 共有JSON取得時はキャッシュを避けるため更新時刻付きURL + `cache: no-store` を使用。
- データ管理のJSON書き出しを「共有用JSONを書き出す」へ変更。
- 現在GitHubにある共有定点と、ブラウザ内のマイ定点をマージして `lineups.json` を生成する処理を追加。
- 生成した `lineups.json` をそのまま `data/lineups.json` へ置き換えられる形式に統一。
- READMEをv0.4仕様へ更新。

### 共有手順
1. サイトで定点を作る。
2. データ管理から「共有用JSONを書き出す」。
3. ダウンロードされた `lineups.json` をGitHubの `data/lineups.json` と置き換える。
4. GitHub Pages反映後、友達が再読み込みすると同じ定点が表示される。

### 保存仕様
- 共通定点: GitHub `data/lineups.json`
- 自作途中 / 個人定点: localStorage
- お気に入り / 設定: localStorage

### 維持した仕様
- GitHub Pages対応
- Node.js / npm不要
- Win + Shift + S → Ctrl + V
- 画像WebP圧縮
- マップ上の立ち位置 / 中継点 / 着弾点
- 編集 / 削除 / お気に入り
- Valorant-API取得とフォールバック

### 注意点
- ブラウザからGitHubへ直接書き込む機能は入れていない。
- GitHubトークンをHTML/JSへ埋め込まない。
- GitHubへ反映済みの定点がlocalStorageにも残っている場合、ライブラリで二重表示されることがある。反映確認後にマイ定点側を削除すれば解消できる。
- GitHub Pages側の反映にはGitHub側のデプロイ時間がかかる場合がある。

### 確認できたこと
- 既存コードでは `data/lineups.json` を起動時にfetchしていることを確認。
- 共有JSONが配列形式であることを確認。
- localStorageの自作定点形式が共有JSONと互換性のある定点オブジェクトであることを確認。

### 未確認
- Windows実機でのGitHub Pages公開URLからの最終操作テスト。
- 友達側ブラウザからの実際の共有反映確認。

---

## 2026-08-27 / v0.5 MP4動画対応

### 依頼内容
- 定点にPC内のMP4動画を登録できるようにする。
- 既存のYouTube / 動画URL機能は維持する。
- GitHub Pagesと友達共有の仕組みを壊さない。

### 変更内容
- 「自分で作る」の動画欄へMP4ファイル選択UIを追加。
- `.mp4` 選択後、その場で動画プレビューできるようにした。
- MP4本体をlocalStorageやJSONへBase64埋め込みしない構成にした。
- ローカルMP4はIndexedDB `lineupLab.videos.v1` へBlobとして保存するようにした。
- 定点の `videoUrl` には `assets/videos/<ファイル名>.mp4` に対応するGitHub Pages上のURLを保存するようにした。
- 「共有用MP4を保存」ボタンを追加。GitHubへアップロードするときに同じファイル名を維持できるようにした。
- MP4解除ボタンを追加。
- 25MBを超えるMP4にGitHubアップロード上の注意を表示するようにした。
- 定点詳細で `.mp4` URLを検出した場合、通常リンクではなくHTML5 videoプレイヤーを表示するようにした。
- 同じブラウザにIndexedDB版がある場合はローカルMP4を優先再生し、ない場合はGitHub Pages上の共有MP4を再生するようにした。
- `assets/videos/.gitkeep` を追加し、共有MP4用ディレクトリをリポジトリへ追加。
- 共有JSONの説明へ「MP4本体は別途 `assets/videos/` へ置く」注意を追加。
- READMEをv0.5仕様へ更新。

### MP4保存仕様
- 定点メタデータ / `videoUrl`: localStorage → 共有時は `data/lineups.json`
- ローカルMP4本体: IndexedDB `lineupLab.videos.v1`
- 友達共有MP4本体: GitHub `assets/videos/*.mp4`

### 共有手順
1. サイトでMP4を選択。
2. 定点を保存。
3. 「共有用MP4を保存」でMP4を書き出す。
4. GitHubの `assets/videos/` へMP4をアップロード。
5. 「共有用JSONを書き出す」で `lineups.json` を作成。
6. GitHubの `data/lineups.json` を置き換える。
7. GitHub Pages反映後、友達側でもMP4を再生可能。

### 守った仕様
- GitHubトークンや秘密鍵は追加していない。
- GitHub Pagesだけで動作する静的構成を維持。
- Node.js / npm不要を維持。
- Win + Shift + S → Ctrl + Vを維持。
- YouTube / 通常の動画URLを維持。
- 既存JSON構造を大幅変更せず `videoUrl` を流用。

### 検証
- 追加したJavaScript相当コードについて `node --check` で構文確認を実施。
- `assets/videos/` ディレクトリ追加を確認。
- READMEと作業報告書を更新。

### 未確認
- Windows実機ブラウザで実際のMP4選択 → IndexedDB保存 → 再読み込み後再生。
- GitHub Pages公開URLでのMP4ストリーミング再生。
- 25MBを超えるMP4のGitHub Web UIからの実アップロード。
- 友達側ブラウザでの共有MP4再生。
