# 作業報告書

## 現在バージョン

**Lineup Lab v0.9.0**

GitHub Pages向けのVALORANT定点管理・共有サイト。

---

## 2026-08-29 / v0.9.0 他GitHubプロジェクトを参考にProduct UX改善

### 依頼内容

- 自分の別GitHubプロジェクトを参考に、Lineup Labをさらに改善する。
- 見た目だけでなく、完成品としての使いやすさ・運用性も上げる。
- 既存の定点作成、MP4、圧縮、共有JSONを壊さない。

### 参考に確認したリポジトリ

- `EliteMay/vreview`
- `EliteMay/lyrictube`
- `EliteMay/asmrtube`
- `EliteMay/ap-study-notes`

### 参考にした点

#### VReview

- 現在バージョンを画面へ常時表示する方式
- 状態・数値を短いカードで見せる方式
- UIと処理ロジックを分ける設計

#### LyricTube

- 11〜14px中心の読みやすいUI密度
- `focus-visible` の明確化
- 操作部品の統一
- `.nojekyll` を含むGitHub Pages運用

#### ASMRTube

- Dashboard stats
- モバイルサイドバードロワー
- データ管理画面内の統計
- 表示設定の保存
- コンパクト表示
- ショートカット表示

#### AP Study Notes

- 大量データをHTMLへ直書きしない
- JSONと表示処理を分離する
- 規模が増えた段階でmanifest / 分割データへ移行できる構成

今回は共有の簡単さを優先し、`data/lineups.json` の分割は行っていない。

### 要件定義

#### 目的

Lineup Labを「見た目を整えた個人サイト」から、日常利用できるVALORANT攻略ツールへ近づける。

#### 使用者

個人・友達同士。

#### 必要機能

- 現在バージョン常時表示
- ライブラリ概要表示
- GitHub共有 / マイ定点 / お気に入り数の確認
- 表示設定
- モバイルドロワー
- キーボードショートカット
- GitHub Pages運用ファイル

#### 画面構成

既存の3画面を維持。

- 定点を見る
- 自分で作る
- マイ定点

ライブラリ上部へ小型のOverviewを追加。

#### データ構成

変更なし。

- 共通定点: `data/lineups.json`
- ローカル定点: localStorage
- MP4: IndexedDB / `assets/videos/`

#### 保存方法

表示設定を既存 `lineupLab.preferences.v1` へ追加。

追加設定:

- `productThumbs`
- `productCompact`

既存map設定とマージして保存する。

#### 崩してはいけない仕様

- GitHub Pagesのみで起動
- Node / npm不要
- 共有JSON方式
- MP4 IndexedDB方式
- GitHub向けMP4圧縮
- 画像貼り付け
- フィルター
- お気に入り
- 編集 / 削除
- 既存座標形式

### 実装内容

#### Product UIレイヤー

追加:

- `assets/css/product-ui.css`
- `assets/js/product-ui.js`

既存v0.8のPremium UIを土台として、その上にProduct UXだけを追加する構成。

#### バージョン表示

- トップバーに `Lineup Lab v0.9.0`
- 画面右下にも小さいバージョン表示

VReviewの常時表示方式を参考にした。

#### Library Overview

ライブラリ上部へ以下を表示。

- Current Map
- Matching
- Shared
- My Lineups
- Favorites

ASMRTubeのDashboard StatsをLineup Lab向けに縮小している。

#### 表示設定

データ管理モーダルへ追加。

- サムネイル表示 ON / OFF
- コンパクト表示 ON / OFF

ブラウザごとに保存。

#### 設定保存の保護

既存 `LineupStorage.savePreferences` をProduct UI起動時にラップし、既存設定と新設定をマージして保存するようにした。

これによりマップ変更時に表示設定が消える問題を防ぐ。

#### キーボード操作

- `/` → 定点検索
- `N` → 新規定点作成
- `Esc` → モバイルドロワーを閉じる

入力欄操作中はショートカットを発火させない。

#### モバイルドロワー

900px以下で:

- 左Railを常時表示しない
- トップバーへメニューボタンを表示
- 左からドロワーとして展開
- 背景クリックで閉じる
- メニュー選択後に閉じる

LyricTube / ASMRTubeのモバイル方式を参考にした。

#### Accessibility / 操作状態

- `focus-visible` を追加
- キーボードフォーカスを赤アクセントで明確化
- 極小文字を一部引き上げ

#### GitHub Pages運用

追加:

- `.nojekyll`
- `.gitignore`

`.gitignore` では以下を除外。

- OS一時ファイル
- editor設定
- node_modules
- ZIP
- log
- ローカル動画退避ディレクトリ

#### ローダー

`assets/js/compressor.js` を更新。

```js
import './product-ui.js?v=0.9.0';
import './compressor-core.js?v=0.9.0';
```

Product UIと圧縮本体を分離したまま読み込む。

### 変更したファイル

追加:

- `.nojekyll`
- `.gitignore`
- `assets/css/product-ui.css`
- `assets/js/product-ui.js`

更新:

- `assets/js/compressor.js`
- `README.md`
- `作業報告書.md`

### 変更していない主要部分

- `data/lineups.json`
- `app.js` の定点処理本体
- `storage.js` のIndexedDB本体
- `compressor-core.js`
- `ffmpeg-worker.js`
- 定点JSON Schema

### 確認できたこと

- 他の所有GitHubリポジトリ一覧を確認した。
- VReview / LyricTube / ASMRTube / AP Study Notesの実ファイルを確認した。
- `product-ui.css` がGitHubへ追加された。
- `product-ui.js` がGitHubへ追加された。
- `compressor.js` がv0.9 Product UIを読み込む構成になった。
- `.nojekyll` が追加された。
- `.gitignore` が追加された。
- READMEをv0.9の現行仕様へ書き直した。
- 共有JSON形式とMP4保存方式は変更していない。

### 確認できなかったこと

- GitHub Pages公開URLでのv0.9最終描画
- Firefox実機でのモバイルドロワー操作
- Chromium実機でのモバイルドロワー操作
- 実MP4のffmpeg.wasm圧縮完走
- 大量定点時のProduct Overview更新負荷

### 今後必要な作業

- GitHub Pages反映後の実画面確認
- スクリーンショットを基に余白・カードサイズを微調整
- 定点が数百件規模になった場合はマップ別JSON / manifest方式を検討

---

## v0.8 Visual Library Redesign

### 主な変更

- MAPを大型化
- 定点カードを参考画像中心へ変更
- 詳細パネル大型化
- エージェント検索追加
- マップ選択を横スクロールカード化
- `premium-v2.css`
- `premium-ui-v2.js`

---

## v0.7 Premium UI

### 主な変更

- 黒寄りのダークUI
- パネル階層整理
- ボタン / input / selectの統一
- マップ / 詳細 / 作成 / マイ定点の見た目調整

---

## v0.6 GitHub向けMP4自動圧縮

### 主な変更

- ffmpeg.wasm
- 24MB以下を目標に自動圧縮
- H.264 + AAC
- 解像度 / bitrate自動調整
- IndexedDBへ圧縮後動画保存

---

## v0.5 MP4対応

### 主な変更

- MP4ファイル選択
- IndexedDB保存
- MP4プレビュー
- `assets/videos/` 共有

---

## v0.4 GitHub共有JSON

### 主な変更

- `data/lineups.json` を全員共通データ化
- 共有用JSON書き出し
- GitHub側データ + マイ定点をマージ

---

## 初期版

- v0.1 初版
- v0.2 Win + Shift + S / Ctrl + V
- v0.3 GitHub Pages対応
