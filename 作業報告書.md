# 作業報告書

## 2026-08-27 / v0.4 GitHub共有JSON対応

### 依頼内容
- 友達同士で同じ定点を見られるようにする。
- GitHubの `data/lineups.json` を全員共通データとして使う。
- アカウント、投稿申請、外部DBは使わない。

### 変更内容
- `data/lineups.json` を全員共通定点として扱う方針を明確化。
- サイト起動時の共有JSON読み込みを維持。
- 共有JSON取得時はキャッシュを避けるため更新時刻付きURL + `cache: no-store` を使用。
- データ管理のJSON書き出しを「共有用JSONを書き出す」へ変更。
- GitHub上の共有定点とブラウザ内のマイ定点をマージして `lineups.json` を生成。

### 保存仕様
- 共通定点: GitHub `data/lineups.json`
- 自作途中 / 個人定点: localStorage
- お気に入り / 設定: localStorage

---

## 2026-08-27 / v0.5 MP4対応

### 依頼内容
- 定点へPC内のMP4動画を登録できるようにする。
- 元ファイルをPCから削除しても、同じブラウザでは再生できるようにする。
- 友達と共有する場合はGitHub PagesからMP4を再生できるようにする。

### 変更内容
- 「自分で作る」の動画欄へMP4ファイル選択UIを追加。
- MP4選択後にサイト内でプレビュー可能にした。
- MP4本体をlocalStorage / JSONへ入れず、IndexedDBへ保存する方式を追加。
- `LineupStorage.getLocalVideo / saveLocalVideo / deleteLocalVideo` を追加。
- MP4共有用URLを `assets/videos/ファイル名.mp4` として自動生成。
- 「共有用MP4を保存」ボタンを追加。
- 「MP4解除」ボタンを追加。
- 定点詳細画面でMP4をHTML5 videoプレイヤーとして再生。
- GitHubに `assets/videos/.gitkeep` を追加。
- YouTube / 通常動画URL機能は維持。

### 保存仕様
- 定点メタデータ: localStorage / `data/lineups.json`
- ローカルMP4本体: IndexedDB
- 共有MP4本体: GitHub `assets/videos/`

### 制約
- ブラウザのサイトデータを削除するとIndexedDBのMP4も削除される。
- 別ブラウザ・別PCにはIndexedDBのMP4は自動同期されない。
- 友達へ共有するにはMP4本体をGitHubへアップロードする必要がある。

---

## 2026-08-27 / v0.6 GitHub向けMP4自動圧縮

### 依頼内容
- MP4をGitHubへ置きやすいサイズまでサイト上で自動圧縮する。
- 操作を難しくせず、おすすめ設定を自動適用する。
- 通常のサイト閲覧を重くしない。

### 要件定義

#### 目的
GitHubのブラウザアップロードで扱いやすいよう、登録済みMP4を24MB以下を目標にブラウザ内で圧縮する。

#### 使用者
Lineup Labを使って友達とVALORANT定点を共有するユーザー。

#### 必要機能
- 登録済みMP4のサイズ確認
- 24MB以下への自動圧縮
- 動画時間から必要ビットレートを自動計算
- 解像度自動調整
- 最大60fps
- 圧縮進捗表示
- 圧縮結果の元サイズ / 圧縮後サイズ表示
- 24MB超の場合の自動再調整
- 圧縮失敗時に元動画を維持

#### 画面構成
既存MP4パネル内に「GitHub向け自動圧縮」セクションを追加。

#### データ構成
圧縮後MP4も既存と同じIndexedDBレコードへ上書き保存し、定点JSONの構造は変更しない。

#### 保存方法
- 圧縮前/後のMP4: IndexedDB
- 共有用MP4: `assets/videos/`
- 定点URL: `videoUrl`

#### 崩してはいけない仕様
- GitHub Pagesだけで動く。
- npm / Node.jsを必須にしない。
- 既存MP4登録・YouTube・画像・JSON共有を壊さない。
- MP4本体をJSONへ埋め込まない。
- 圧縮エンジンを通常閲覧時に読み込まない。

#### 完成条件
- 圧縮UIが表示される。
- MP4未選択時は圧縮ボタンが無効。
- 24MB以下なら圧縮不要と判定。
- 24MB超なら自動設定で圧縮処理を開始できる。
- 圧縮後をIndexedDBへ保存する設計になっている。
- README / 作業報告書が更新されている。

### 実装内容
- `assets/js/compressor.js` を追加。
- `assets/js/ffmpeg-worker.js` を追加。
- `storage.js` から圧縮モジュールを読み込むよう変更。
- 圧縮実行時のみ `@ffmpeg/ffmpeg 0.12.15` と `@ffmpeg/core 0.12.10` をCDNから取得。
- GitHub Pagesの特殊ヘッダーを不要にするためsingle-thread coreを採用。
- H.264 / libx264 + AAC 96kbpsでMP4出力。
- 目標サイズ24MB、内部目標約22.5MB。
- 動画時間から映像ビットレートを自動算出。
- ビットレートに応じて最大1920 / 1280 / 960 / 854px幅へ自動調整。
- 最大60fps。
- 1回目で24MBを超えた場合、ビットレートを再計算して自動で1回再圧縮。
- 300MB超はブラウザ安定性を優先して処理対象外。
- 圧縮後が元動画より大きい場合は元動画を維持。
- 進捗バーと結果表示を追加。
- UI監視による再描画ループが起きないよう監視対象を整理。

### 外部ライブラリ
- `@ffmpeg/ffmpeg 0.12.15`
- `@ffmpeg/core 0.12.10`
- x264 / H.264

### 確認できたこと
- GitHubに `compressor.js` / `ffmpeg-worker.js` が作成された。
- `storage.js` から圧縮モジュールを読み込むコードが追加された。
- GitHub Pages互換の相対パス構成を維持している。
- MP4本体をJSONへ埋め込まない仕様を維持している。
- READMEをv0.6仕様へ更新した。

### 確認できなかったこと
- Windows実機のGitHub Pages公開URLでのffmpeg.wasm実圧縮完走。
- Firefox / Chromiumそれぞれでの実MP4圧縮速度。
- すべてのMP4コーデックでの入力互換性。
- 24MB付近の動画が常に1回の再調整で24MB以下になること。

### 注意点
- 初回圧縮時は約31MBのFFmpeg core取得が発生する。
- 圧縮はCPU / メモリ負荷が高い。
- 動画の長さ・解像度・コーデックによって処理速度が大きく変わる。
- 圧縮中にページを閉じると処理は中断される。

---

## 2026-08-27 / v0.7 プレミアムUI全面調整

### 依頼内容
- サイト全体に残っている安っぽさを減らす。
- 見た目を良くしても既存機能・軽量性・操作性を悪化させない。

### 要件定義

#### 目的
単純な濃紺カードの集合に見えていたUIを、VALORANT系の攻略ツールとして見栄えと情報階層が明確なUIへ改善する。

#### 使用者
友達同士でLineup Labを利用するユーザー。

#### 必要機能
- 新機能追加は行わない。
- ライブラリ / 作成 / マイ定点 / モーダルの見た目を統一する。
- フィルター・ボタン・入力欄の状態を分かりやすくする。
- マップと詳細情報を視覚的に主役として扱う。
- 小さい文字を減らして読みやすくする。
- 1280px前後から小さい画面まで致命的なレイアウト崩れを避ける。

#### 画面構成
既存画面構成を維持し、DOMの大幅変更は行わない。

#### データ構成
変更なし。

#### 保存方法
変更なし。

#### 崩してはいけない仕様
- `data/lineups.json` 共有
- localStorageの自作定点
- IndexedDBのMP4
- Win + Shift + S → Ctrl + V
- MP4登録 / 自動圧縮
- YouTube / 動画URL
- フィルター / お気に入り / 編集 / 削除
- GitHub Pages対応

#### 完成条件
- プレミアムUIがサイト起動時に読み込まれる。
- 既存圧縮ロジックは同じ内容のまま維持される。
- README / 作業報告書を更新する。
- 主要画面にレスポンシブ条件を持たせる。
- 実機未確認事項は確認済み扱いにしない。

### 安っぽさの主な原因として確認した点
- 8〜10pxの極小文字が多かった。
- パネル・ボタン・入力欄が似た濃紺 + 1px枠で、階層差が弱かった。
- 角丸・境界線・背景の質感が似通っていた。
- 赤アクセントが主に単純な塗りつぶしで使われていた。
- マップ、詳細、カードの重要度差が見た目に十分出ていなかった。

### 実装内容
- `assets/js/premium-ui.js` を追加。
- 背景を黒寄りのニュートラルなダークUIへ調整。
- パネルは薄いハイライト・影・ヘアラインで階層を明確化。
- サイドナビ / トップバー / タブのアクティブ状態を再設計。
- ボタンをグラデーション・押下状態・フォーカス状態まで統一。
- 検索欄 / select / textarea / filter UIを統一。
- マップ画像のコントラスト、ビネット、グリッド、ルート線、ピンを調整。
- 定点カードの余白、文字サイズ、アバターサイズ、選択状態を改善。
- 詳細パネルのエージェントヒーロー、ルート、画像、動画、アクションを調整。
- 作成フォーム、ルートモード、画像枠、MP4、圧縮UIの質感を統一。
- マイ定点カード / データ管理モーダル / Toastを再設計。
- 1550 / 1280 / 1020 / 720px帯のレスポンシブ調整を追加。
- `prefers-reduced-motion` 対応を追加。
- 重い画像背景や常時動作アニメーションは追加していない。

### 圧縮機能への影響を避ける対応
- 既存 `compressor.js` の実装内容をそのまま `compressor-core.js` として保持。
- 新しい `compressor.js` は `premium-ui.js` → `compressor-core.js` の順で読み込む軽量ローダーへ変更。
- 圧縮設定・FFmpeg処理・IndexedDB処理のロジック自体は変更していない。

### 変更したファイル
- `assets/js/premium-ui.js` 追加
- `assets/js/compressor.js` ローダー化
- `assets/js/compressor-core.js` 追加（既存圧縮本体）
- `README.md` 更新
- `作業報告書.md` 更新

### 確認できたこと
- GitHub上で `premium-ui.js` が存在する。
- `compressor.js` が `premium-ui.js` と `compressor-core.js` を読み込む構成になっている。
- `compressor-core.js` のblobは変更前 `compressor.js` と同一で、圧縮本体を保持している。
- 既存HTML / app.js / storage.js / lineups.json は変更していない。
- 1280px以下を含む複数のCSSブレークポイントを追加した。
- READMEをv0.7へ更新した。

### 確認できなかったこと
- GitHub Pages上での最終視覚確認。
- Firefox / Chromium両方でのレイアウト最終確認。
- 実際のVALORANT API画像が入った状態での全マップ・全エージェント表示確認。
- Windows実機でのMP4圧縮完走は引き続き未確認。

### 今後必要な作業
- GitHub Pages反映後に画面を確認し、気になる部分をスクリーンショット単位で詰める。
- 実際の使用画面で余白・カード密度・詳細パネル幅を必要に応じて微調整する。
