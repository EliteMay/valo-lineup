# Lineup Lab 作業報告書

## 現在バージョン

**v1.2.1 / 2026-09-01**

## 今回の目的

v1.2.0を「機能がある状態」から、**完成判定に耐えられる状態へ近づけるRegression Hardening**を実施しました。

最新 `web-project-guide` の以下を基準にしています。

- Architecture: Renderer owns its DOM / Patchを恒久化しない
- Testing / Quality: Static + Browser Smoke + Final-state Validation
- Visual Quality Baseline: 見た目変更後はScreenshot / Browserで確認
- Maintenance: Version / Runtime / Cacheの正本を整理する

---

## 実施したこと

### 1. Chromium Browser Smoke Testを追加

追加:

- `tests/browser-smoke.spec.js`
- `.github/workflows/browser-smoke.yml`

Chromiumで以下を自動確認します。

```text
ページ起動
→ 作成画面へ移動
→ Start / EndをMapへ配置
→ Screenshot画像登録
→ 定点保存
→ localStorage保存確認
→ 再読込
→ マイ定点復元
→ 詳細表示
```

追加確認:

- Desktop horizontal overflow
- Mobile horizontal overflow
- Navigation drawer
- Filter drawer
- Drawer geometryがViewport内にあること
- JavaScript page error
- Console error
- Desktop / Mobile screenshot artifact

### 2. Browser Testで実バグを検出

最初のSmoke Testで、390px幅のMobile Filter Drawerを開いてもPanelが画面外に残る問題を検出しました。

原因:

`lineuplab-v2` classは `<html>` に付いているのに、CSSが以下のように `body` 内の子孫classとして指定していました。

```css
body.filter-drawer-open .lineuplab-v2 .filter-panel
```

同じ問題がNavigation Drawerにもありました。

修正後:

```css
html.lineuplab-v2 body.filter-drawer-open .filter-panel
html.lineuplab-v2 body.nav-drawer-open .rail
```

### 3. Browser Testの誤検知を修正

Valorant APIのOffline fallbackを確認するため通信をabortしていましたが、Chromium Consoleの `ERR_FAILED` までアプリErrorとして検出していました。

外部APIを空の正常Responseへ置換し、アプリ自身のConsole Errorだけを検出できるようにしました。

### 4. Visual Review Evidenceを取得

PlaywrightのScreenshot Artifactで実際のBrowser描画を確認しました。

Desktopで確認した内容:

- Tactical MapがVisual hierarchyの主役になっている
- Filter / Card / Detailが同じ強さで競合していない
- 大量のGlow / Shadow / Rounded Cardへ戻っていない
- Main navigation / Creator / Mineへの導線が見える

Mobile初回ScreenshotからDrawer不具合を発見できたため、Static Validationだけでは検出できないVisual RegressionをBrowser Testへ追加しました。

### 5. Static Validationは維持

既存 `.github/workflows/validate.yml` では以下を継続確認します。

- 共有JSON
- ID重複
- 必須項目
- 座標
- Data URL / ローカル画像参照の共有JSON混入
- 主要JavaScript / Service Worker構文

---

## 変更ファイル

追加:

- `tests/browser-smoke.spec.js`
- `.github/workflows/browser-smoke.yml`

変更:

- `assets/css/foundation-v1.css`
- `README.md`
- `作業報告書.md`

---

## 保存互換性

今回、以下は変更していません。

- `lineupLab.userLineups.v1`
- `lineupLab.favorites.v1`
- `lineupLab.preferences.v1`
- IndexedDB `lineupLab.images.v1`
- IndexedDB `lineupLab.videos.v1`
- `data/lineups.json` のトップレベル配列
- 共有ZIP形式
- MP4圧縮仕様

---

## 現在残っている項目

### Architecture debt

`premium-ui-v2.js` は以下をMutationObserverで後付けしています。

- Card thumbnail
- Agent search
- Detail label
- Map watermark

`app.js` の正式renderへ統合するのが理想ですが、45KBのApp本体を完成直前に無検証で一括置換する方がRegression Riskが高いため、今回まずBrowser Regression Oracleを固定しました。

次回統合時はこのSmoke Testを維持したまま変更します。

### Content

`data/lineups.json` はデモ中心です。

正しい照準位置 / Charge / Bounceを確認できない実戦定点は推測で追加していません。

### Real-device verification

CI Chromiumとは別に未確認:

- ユーザーのWindows / Firefoxで全導線
- 実MP4を使用したffmpeg.wasm圧縮完走
- 共有ZIPを別PCへ反映した実利用
- 100件以上の画像を使った長期利用

---

## 完成判定について

技術面では、主要機能 + Static Validation + Chromium Browser Smoke + Visual Screenshot Evidenceまで揃える工程へ進めています。

ただし次の2点は分けて扱います。

- **アプリ基盤の完成度**: 完成工程へ到達可能
- **定点コンテンツの完成度**: 実用定点データ不足のため未完成

未確認項目を確認済みとして扱いません。

---

## 変更履歴

- v1.0.0 — 保存 / 共有 / Validation基盤整理
- v1.1.0 — 画像IndexedDB / Service Worker / 共有画像ZIP
- v1.2.0 — Tactical Workspace Visual Redesign
- **v1.2.1 — Browser Smoke / Mobile Drawer Regression Hardening**
