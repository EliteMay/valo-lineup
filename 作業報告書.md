# Lineup Lab 作業報告書

## 現在バージョン

**v1.0.0 / 2026-08-30**

## 今回の目的

v0.9を「完成品ではなく、まだ作りが甘いサイト」として再監査し、見た目以外の欠点も含めて修正しました。

優先したのは以下です。

1. 壊れやすい読み込み構造
2. データ共有事故
3. IndexedDBのMP4残骸
4. JSON破損・不正データ
5. 900〜1180px付近の使いにくさ
6. 小さすぎる文字
7. 過剰なv0.9ダッシュボードUI
8. テスト不足

## 今回変更した内容

### 1. UIとMP4圧縮を分離

変更前:

```text
storage.js
└─ compressor.js
   ├─ product-ui.js
   │  └─ premium-ui-v2.js
   └─ compressor-core.js
```

変更後:

```text
index.html
├─ storage.js
├─ app.js
└─ foundation-v1.js
   └─ premium-ui-v2.js

storage.js
└─ compressor.js
   └─ compressor-core.js
```

UIが動画圧縮ローダー経由でしか起動しない問題を修正しました。

### 2. v0.9 Product UIを削除

削除:

- `assets/js/product-ui.js`
- `assets/css/product-ui.css`

v0.9で入れた大きい統計カード・二重バージョン表示も廃止しました。

### 3. v1.0 Foundation UIを追加

追加:

- `assets/js/foundation-v1.js`
- `assets/css/foundation-v1.css`

役割:

- バージョン表示
- 表示設定
- 保存量表示
- GitHub共有ON/OFF
- 共有JSON安全化
- JSON読み込み検証
- MP4孤児データ削除
- キーボードショートカット
- ナビドロワー
- フィルタードロワー
- 文字サイズ補正

### 4. GitHub共有を明示選択式へ変更

「マイ定点」カードへ **GitHub共有** ON/OFFを追加しました。

共有JSON書き出し時:

- ONだけ追加・更新
- OFFへ変更した既存共有定点は削除候補
- 出力件数を事前確認

作りかけのマイ定点をすべて自動共有する仕様を廃止しました。

### 5. MP4残骸削除

`LineupStorage.saveUserLineups` / `clearUserLineups` をv1.0基盤側で保護し、

- 定点削除
- 全削除
- MP4差し替え

で参照されなくなったローカルMP4をIndexedDBから削除するようにしました。

### 6. localStorage容量ガード

画像Data URLを含むローカル定点JSONが約4MBを超える場合、保存を失敗扱いにしてブラウザ容量限界へ突っ込むのを防ぐガードを追加しました。

データ管理画面へ保存量表示も追加しました。

### 7. JSON読み込み検証

サイトへJSONを取り込む前に、

- 必須文字列
- start/end座標
- bounces
- videoUrl

を検証します。

不正データはそのまま保存しません。

### 8. GitHub Actions検証

追加:

- `.github/workflows/validate.yml`
- `tests/validate-data.mjs`

`data/lineups.json` 変更時に、

- トップレベル配列
- ID重複
- 必須項目
- 座標範囲
- side / difficulty
- tags / images / videoUrl

をNode標準機能だけで検査します。

### 9. 1180px以下の操作性改善

- 左ナビをドロワー化
- フィルターを右ドロワー化
- MAPを先に見やすい構成へ変更
- 小画面で定点カードを1列化

### 10. UI文言・文字サイズ

- 「おすすめ順」→「標準順」
- 8〜9px中心の補助文字を減らす
- バージョン表示を1か所だけにする
- v0.9の大きい概要統計を削除

### 11. キャッシュ対策

`index.html` から読み込む主要CSS/JSへ `v=1.0.0` を付けました。

## 今回変更したファイル

- `index.html`
- `README.md`
- `作業報告書.md`
- `assets/js/compressor.js`
- `assets/js/foundation-v1.js` 新規
- `assets/css/foundation-v1.css` 新規
- `tests/validate-data.mjs` 新規
- `.github/workflows/validate.yml` 新規

削除:

- `assets/js/product-ui.js`
- `assets/css/product-ui.css`

## 維持した機能

- GitHub Pages
- `data/lineups.json`
- マップ / エージェント / アビリティフィルター
- マップ上の定点表示
- 定点作成・編集・削除
- スクリーンショット貼り付け
- localStorage
- IndexedDB MP4
- YouTube / 動画URL
- MP4プレイヤー
- ffmpeg.wasm自動圧縮
- GitHub共有JSON
- お気に入り

## 未完了の項目

### 1. 画像のIndexedDB化

画像はまだData URLをlocalStorageの定点JSONへ保持しています。

今回は既存データを壊さないため、

- 容量ガード
- 使用量表示
- 注意表示

までに留めました。

将来は画像をIndexedDBへ移し、共有画像を `assets/lineups/<id>/` へ置く方式が必要です。

### 2. app.jsとPremium UIの完全統合

v0.9からUIレイヤーは1枚減りましたが、`premium-ui-v2.js` のカード装飾などはMutationObserver方式のままです。

今後は `app.js` のrender処理へ統合した方が保守しやすいです。

### 3. 実用定点データ不足

`data/lineups.json` はデモ定点中心です。

実戦で使える定点は、正しい照準位置・チャージ・バウンドなどを確認したうえで追加する必要があります。未確認の実戦定点を捏造して追加していません。

## できなかったこと

- Windows実機からのGitHub Pagesブラウザ操作
- 実機でのffmpeg.wasm圧縮完走
- Firefox / Chromium両方での最終視覚確認
- 大量画像100件以上を使った長期容量試験

## 確認したこと

- v1.0用JS/CSSがGitHubに存在する
- v0.9 Product UI JS/CSSが削除されている
- `compressor.js` が圧縮本体だけを読み込む
- `index.html` がFoundation UIを直接読み込む
- `.nojekyll` / `.gitignore` が存在する
- GitHub Pages buildがv1.0のindex変更後に成功している
- JSON検証用テスト・workflowがリポジトリに存在する

## 今後必要な作業

優先順:

1. 実ブラウザでv1.0画面確認
2. マイ定点のGitHub共有ON/OFF確認
3. 定点削除後のIndexedDB MP4削除確認
4. JSON読み込み検証確認
5. ffmpeg圧縮確認
6. 画像IndexedDB移行設計
7. app.js / premium-ui-v2.js統合
8. 実用定点データ追加

## 変更履歴

- v0.1 — 初版
- v0.2 — スクリーンショット改善
- v0.3 — GitHub Pages
- v0.4 — 共有JSON
- v0.5 — MP4 / IndexedDB
- v0.6 — MP4自動圧縮
- v0.7 — Premium UI
- v0.8 — MAP / カード / 詳細再設計
- v0.9 — 他プロジェクトのUIパターン導入
- **v1.0.0 / 2026-08-30 — 監査結果を反映した基盤修正**
